
// Write your code here

// Assign values to num1 and num2 so their multiplication equals 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // generates a random integer between 1 and 100

// Assign values to num3 and num4 so their modulo operation equals 4
const num3 = 16;
const num4 = 6;
const mod = num3 % num4; // 16 % 6 equals 4

// Find the highest number in a set
const max = Math.max(1, 5, 10, 15, 20); // returns 20

console.log(multiply); // Should output: 62
console.log(random); // Should output a random integer greater than 0
console.log(mod); // Should output: 4
console.log(max); // Should output: 20
